{
    "db": "$DATABASE_URL",
    "sites": ["heroku-wicked-coolkit.herokuapp.com"],
    "host": {
        "name": "0.0.0.0",
        "port": "$PORT"
    },
    "salesforce": {
        "username": "$SALESFORCE_USER",
        "password": "$SALESFORCE_PASSWORD",
        "url": "$SALESFORCE_URL"
    }
}
