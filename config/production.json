{
    "db": {
        "connectionString": "$DATABASE_URL",
        "ssl": {
            "rejectUnauthorized": false
        }
    },
    "host": {
        "name": "0.0.0.0",
        "port": "$PORT"
    },
    "salesforce": {
        "loginUrl": "$SALESFORCE_URL",
        "authUrl": "$SALESFORCE_AUTHURL"
    }
}
